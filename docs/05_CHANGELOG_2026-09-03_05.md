# LOS 시뮬레이터 변경 내역 (2026-09-03 ~ 09-05)

> 골격만 먼저 잡은 문서. 각 꼭지의 "내역"은 추후 채움.
> 근거 커밋: 3.8.2 (09-05 07:45, 배치 CLI) → 3.8.3 (10:51, 지표 통일) → 3.8.4 (11:44, 후보점 m 단위) → 미커밋(워커 전환·베란다 후보 중복 제거)

## 1. 전체리스트 대량 배치 — 터미널 CLI (3.8.2)
- `scripts/batchCli.ts` (`npm run batch:run`): concurrency / filter / skipAnalyzed / force / limit / keys 옵션
- `server/batchManager.ts` + `scripts/losRunner.ts`: fork 워커 · IPC 결과 · 파일 폴백
- 폴리곤 미확보 단지 사전 분리(skipped) — 에러 폭탄 방지
- 내역: (추후)

## 2. 대시보드(전체 리스트) 개선 (3.8.2)
- 폴리곤 확보 상태 필터(전체 / 미확보만 / 미확보 제외)
- 실행 중 게이지바 · 대기 큐 순번 · 완료 단지 실시간 동기화
- 내역: (추후)

## 3. 커버율 지표 통일 — (1차 + 0.7×2차) / 1차 총량 (3.8.3)
- 원인: Phase 2 루프·다이어트는 1차만 집계, 최종 KPI는 2차를 1.0으로 가산 → 2202-0846 수동 63% vs 자동 88.7% 과설계
- `SECOND_VERANDA_WEIGHT = 0.7` 단일 상수, 루프 종료·dietFloor·최종 KPI·건물별 커버리지 동일 적용
- Phase 3.5 일반 2차 확장 섹터 목표 게이트(EXPAND-SKIP), SAVIOR는 유지
- 검증: 2202-0846 88.7%/6site/14sector → 63.5%/4site/10sector (수동안 4site/9sector와 동급)
- 부수 효과: 기존 보고값 하향(2차 70% 반영) → 배치 재실행 필요
- 내역: (추후)

## 4. 탐색 병목 — 후보점 간격 미터 단위 동적화 (3.8.4)
- 4px 고정 → `candidateStepMeters = 2.5` (축척 px/m 환산, 단지별 0.5~4.5m 편차 해소)
- `[후보점]` 로그 · `searchSummary`("X개 포인트 × N회 × 36방향 = Z개 케이스") · RESULTS 카드 문장
- 검증: 결과 동일, 후보 1,900→792, 시간 −30%
- 내역: (추후)

## 5. 탐색 병목 — 베란다 선 4px 중복 후보 제거 (미커밋)
- 테두리 후보와 겹치는 베란다 후보 1,262개(64%) 제거 → step 격자 셀 점유 시 스킵
- 검증: 축소 장면 후보 995→345, 시간 −42%, 커버리지 동일
- 내역: (추후)

## 6. 단일 분석(플로팅 버튼) 별도 프로세스화 (미커밋)
- `/api/sim/run-single` 인라인 실행 → `scripts/simWorker.ts` fork + jobId 폴링 (`/api/sim/job/:id`, cancel)
- 브라우저 fallback은 서버 미연결 시에만 (job 실패 시 재연산 금지)
- 구버전 서버 응답 호환, "(수동 분석 결과)" → "(화면 분석 결과)" 라벨
- 내역: (추후)

## 7. 문서 정비 — CAD·LightGBM 파이프라인 지시서 (3.8.3)
- docs/01~04: Gemini 구현 / Claude 리뷰 역할 분리, 기존 `pyApi.ts`·별도 Flask 리포와 정합, 맥북 경로는 참조 전용 명시
- 내역: (추후)

## 8. 사내 배포 이슈 (미해결, 보류)
- DIY `entrypoint: none` nginx 정적 전용 → Node 없음 → `/api/*`(단일·배치·results) 사내 미동작
- 해결 후보: Node 서비스화(entrypoint `tsx server.ts`) + 결과 JSON DB화
- 내역: (추후)

## 9. 검증 하니스 메모
- 맥 VM node_modules는 darwin(esbuild) → 클라우드 컨테이너에 src/lib+scripts+temps 스테이징 후 `tsx scripts/losRunner.ts <key>`
- 2202-0846 실측: 구 1450s → 지표통일 2027s → 후보 m단위 1421s → (중복 제거 측정 중)
- 내역: (추후)
