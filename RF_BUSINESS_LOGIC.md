# RF-SIM 비즈니스 로직 및 알고리즘 규격서 (Business Logic Specification)

본 문서는 RF장비 시뮬레이션 및 자동 배치 프로그램(RF-SIM)의 물리적 기준, 알고리즘 연산 정책, 입력 데이터 조건, 산출 결과물 규격을 정리한 공식 기술 명세서입니다.

---

## 1. 기본 그라운드 룰 (Ground Rules)

### 1) 단위 및 공간 스케일
* **지도/화면 좌표계**: 픽셀(Pixel) 단위를 기반으로 직교 가상의 공간을 정의합니다.
* **해상도 비율 (pixelsPerMeter)**: `1m = 2px` (기본값)을 기준으로 실전 거리를 변환합니다. 예: 100m = 200px.

### 2) 장비(Equipment) 및 국소(Site) 개념
* **장비 (Equipment)**: 단일 지향성 안테나(Sector 단말)를 의미합니다.
* **국소 (Site / Pole)**: 안테나가 설치되는 하나의 기둥이나 지지대(폴대)를 의미하며, **무조건 3분기(3-Sectors) 배치를 원칙**으로 고정합니다.

### 3) 3분기 안테나 배치 규칙 (★ 신규 구현)
* **3분기 안테나 고정 규칙**: 하나의 국소(동일한 X, Y 좌표)에는 반드시 **3개의 안테나 장비(Sectors A, B, C)**가 세트로 입지합니다.
* **지향각 지침**: 기본 설치 배치각은 **0도(북쪽), 120도, 240도** 방향으로 360도를 빈틈없이 분할 지향하는 대칭 구조를 형성합니다.
* **각도 이격 제한 조건**: 
  * 사용자가 안테나 방향각을 임의조정(Manual Edit)할 수 있으나, 동일 위치에 서 있는 섹터 안테나끼리 지향 방향이 겹치는 비효율을 방지하기 위해 **최소 45도 이상 거리가 이격**되어야 합니다.
  * 각도 차이가 45도 미만으로 좁혀질 경우, 중복 및 간섭 방지 경고와 함께 조정이 취소되거나 제한됩니다. (※ 이 제한 거리는 추후 기획 및 운영 요구사항에 따라 확장 및 변경 가능)

---

## 2. 타겟 커버리지 및 등급 분류 (Target Targets & Grades)

RF 신호의 전파 투과율 및 반사 요소를 감안하여 건물 외벽의 각 부위를 다른 가중치 등급으로 분류해 LOS(Line of Sight) 점수를 연산합니다.

```
       [건물 Polygon]
     ┌────────────────┐
 ───▶│   긴면 (Long)  │ ───▶ Second 베란다 (70% 점수 인정)
     │                │
 ───▶│ 짧은면(Short)  │ ───▶ 콘크리트 옹벽 (노카운트 - 분석 제외)
     └────────────────┘
```

### 1) 1등급 타겟: 일반 베란다 (Primary Veranda)
* **설명**: 사용자가 지도에 수동으로 그린 면(또는 GIS 로 지정된 정밀 베란다 속성).
* **점수 가중치**: **100% 점수 인정 (가중치 1.0)**
* **의미**: 유리창으로 구성되어 전파 인입이 용이하기 때문에 시뮬레이션 우선순위가 가장 높은 핵심 커버리지 영역입니다.

### 2) 2등급 타겟: Second 베란다 (긴 면의 벽) (★ 신규 구현)
* **설명**: 건물의 긴 옆면을 구획하는 주 벽체 영역입니다.
* **판정 기준**: 
  * 개별 건물 다각형(Polygon)을 구성하는 수 수십개의 외벽 모서리(Edge) 길이를 모두 측정합니다.
  * 해당 건물의 **평균 외벽 모서리 길이 이상**인 수평 벽체들을 자동으로 추출하여 **Second 베란다**로 등급 분류합니다.
* **점수 가중치**: **70% 점수 인정 (가중치 0.7)**
* **의미**: 콘크리트 혼합 벽체이지만 주 면적을 넓게 차지하므로 전파 인입의 차순위 대상지로 포함합니다. 베란다 유리창에 비해 콘크리트 투가 손실이 있으므로 점수 산정 기여도를 70%로 낮춰, 시뮬레이터가 1순위 베란다를 먼저 정밀 지향하도록 설계합니다.

### 3) 3등급 타겟: 짧은 측면 벽 (짧은 벽면)
* **설명**: 한 건물의 **평균외벽 길이보다 짧은 세로축 마감벽체**입니다.
* **판정 기준**: 측면 구조 마감재 영역으로 분류됩니다.
* **점수 가중치**: **0% (노카운트 / 수집 대상 제외)**
* **의미**: 전형적인 일체형 고밀도 콘크리트 벽(옹벽)으로 전파 통과율이 0에 수렴하여 분석 대상 샘플링에서 아예 제거합니다.

---

## 3. 전파 감쇄 및 평가 엔진 룰 (RF Propagation Rating Logic)

안테나에서 송출되는 전파는 다음과 같은 하이브리드 연산 룰을 적용해 수신 여부를 결정합니다.

1. **거리 감쇄 (f_d Limit)**
   * 안테나와 샘플링 좌표 간의 거리가 멀어질수록 감쇄가 발생합니다.
     * $d \le 70m$: 100% 신호 유지 ($1.0$)
     * $70m < d \le 100m$: 90% 신호 세기 ($0.9$)
     * $100m < d \le 150m$: 85% 신호 세기 ($0.85$)
     * $d > 150m$ ($maxRange$): 신호 두절 ($0$)

2. **지향 각도에 따른 손실 감쇄 (Angular Loss)**
   * 안테나 빔폭(Beam Width) 내의 범위에서는 원활히 수신되며, 정중앙 기준 바깥으로 각도가 멀어질수록 손실이 점진적으로 증가하는 Cosine 연산을 수행합니다.
   * 주 빔폭 바깥 $15^\circ$ 누설 범위(Leakage Side lobe)까지는 저하된 지향 이득으로 일부 연결을 확보하나, 그 이상을 넘어가면 신호 수신이 완전히 차단됩니다.

3. **건물 장애 통과 (LOS Obstacle Block)**
   * 기지국 안테나와 타겟 건물 벽면 샘플 사이를 연결한 가상의 전파 직선(Ray) 상에 **다른 건물 다각형 내부**를 가로지르는 간섭 경로가 검출될 경우, 신호 수신이 불가능한 전파 음영 지역(Blocked / No LOS)으로 간주하여 점수를 $0$ 처리 합니다.

4. **수신각 각도 판정 (Normal Cosine Rule)**
   * 벽면 면외 법선(Outward Unit Normal Vector) 방향과 전파 도달 벡터의 사잇각이 입사각 기준 방향을 향할 때만 수신을 성립시킵니다 ($cos(\phi) > 0$). 반대편에서 건물을 뚫고 전판이 들어오는 비현실적 오버랩을 연산상으로 배제합니다.

---

## 4. 입지 선택 및 알고리즘 최적화 과정 (Optimizer Process)

전체 타겟 커버리지 조건(예: $65\%$)을 충족하기 위한 입지 선택 최적화 과정은 다음과 같이 작동합니다.

### Step 1. 전파 장비 연산 준비
1. 사용자가 업로드한 VWorld GIS 지적 또는 직접 그린 건물(Polygon)을 파싱합니다.
2. 각 건물 벽면 중 **Primary Verandas** 와 **Second Verandas (긴 벽면)**를 자동/수동 분류하여 1미터 조밀 간격마다 전파 수신용 **타겟 샘플 좌표**들을 생성합니다.
3. 기지국 기둥(Pole)을 설치할 수 있는 후보 위치(candidatePoints)들을 건물 경계면에 일정 간격으로 전송/생성합니다.

### Step 2. 수동 기지국 장비 우선 평가
* 사용자가 마우스를 이용해 수동으로 지도상에 선언한 기지국(Manual Node Site)이 존재할 경우, 그 기지국의 3개 안테나 전파 도달 영역을 선제적으로 시뮬레이션하여 샘플들을 '수신 완료' 처리하고 달성 점수를 합산해 둡니다.

### Step 3. 탐욕 최적화 루프 (Greedy Multi-Sector Placement Loop) (★ 신규 구현)
* 기 수립된 가중치 누적 점수합(Covered Weight Score)이 전체 타겟 목표값($targetCoverage$)보다 미달한 경우 아래 과정을 만족할 때까지 루프를 가동합니다.
  1. 기둥 설치 후보 위치(`candidatePoints`) 전체를 순회합니다.
  2. 한 후보 위치 $P$에서 **0도에서 110도 사이**로 안테나 방향각 기준($\theta$)을 10도 단위로 회전시키며, 삼방향 빔 세트 $\{\theta, \theta+120^\circ, \theta+240^\circ\}$ 이 커버하는 **나머지 미수신 샘플들의 신규 가중 점수합**을 각각 산출합니다.
  3. 전체 후보지 중 **동일 3개 안테나 한 조 배치로 가장 압도적인 신규 커버리지 가중치 점수 합**을 기록한 "최적의 장소 $P$"와 "배치 기저 각도 $\theta$" 조합을 최종 선택합니다.
  4. 선택한 $P$ 지점에 $3$개의 안테나 장비를 동시에 전개하고, 기둥(Physical Node Site) 1개가 완전히 수립되었음을 시스템 지도에 갱신합니다.
  5. 기지국 중복 최소화 규칙(`strictCoLocation`) 활성화 시, 타 건물 수신 목적으로 동일 기둥을 재사용 제안할 경우 수학 가중치를 추가 가산하여 기둥 수량을 글로벌에서 최소화하도록 유도합니다.

---

## 5. 산출 결과물 규격 (Deliverables Specification)

시뮬레이션 완료 시 시스템은 다음 데이터를 산출하여 다운로드 및 보고서 형태로 제공합니다.

1. **안테나 국소 배치 통계 (Site Properties)**
   * 수립된 최소 기소 국소(Pole Sites) 리스트 및 각 국소별 안테나 번호(A/B/C) 배치 각도.
2. **커버리지 달성률**
   * $\text{최종 달성 커버리지 Ratio (\%)} = \left(\frac{\text{만족한 샘플들의 가중치 점수합}}{\text{전체 수립가능한 원본 샘플 전체 가중치 점수합}}\right) \times 100\%$
3. **통합 분석 로그 (Activity Logs)**
   * 각 기지국 설치 과정의 계산 로그, 획득 점수, 건물별 수신 정밀도 표출.
4. **GIS 내보내기 (GeoJSON Export)**
   * 시뮬레이션된 장비 안테나 좌표 및 지향 속성, 커버리지 포인트 세트가 원본 공간 지리 데이터 규격인 GeoJSON 파일로 반환되어 타사 GIS 툴(QGIS 등)에서 로드 연동할 수 있도록 제공합니다.
